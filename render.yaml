services:
  - type: web
    name: ile-des-rescapes
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /api/admin/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
